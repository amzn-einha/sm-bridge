services:
    aws.greengrass.clientDevices.sm.bridge:
        configuration:
            brokerServerUri: 'tcp://localhost:8883'
    main:
        dependencies:
            - aws.greengrass.clientDevices.sm.bridge
           # - aws.greengrass.StreamManager