services:
    aws.greengrass.smbridge:
        configuration:
            brokerServerUri: 'tcp://localhost:8883'
        dependencies:
            - aws.greengrass.StreamManager
    main:
        dependencies:
            - aws.greengrass.smbridge
           # - aws.greengrass.StreamManager